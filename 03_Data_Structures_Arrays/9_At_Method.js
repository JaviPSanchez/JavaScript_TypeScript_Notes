//If we want to access the last element of an Array:

let arr = [3, 4, 6, 8];
let lastElement = arr[arr.length - 1];
console.log(lastElement);

let arr2 = [3, 4, 6, 8];
let lastElement2 = arr2.flat(-1);
console.log(lastElement2);
