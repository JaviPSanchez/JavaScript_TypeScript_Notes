//Longhand

if(
    value === 1 ||
    value === "one" ||
    value === 2 ||
    value === "two"
) { console.log('Two many conditions')}

//Shortcut

if([1, "one", 2, "two"]).includes(value){ console.log('Better like this')}